[package]
name = "tuile-camera"
description = "Render-agnostic globe camera + input controller (ellipsoid-pick drag, altitude zoom, tilt) producing tuile-core ViewStates"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
glam = { workspace = true }
tuile-core = { version = "0.1.0", path = "../tuile-core" }
